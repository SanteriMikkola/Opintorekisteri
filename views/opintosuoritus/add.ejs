<!doctype html>
<html lang="en">
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
	<title>Add Opintosuoritus</title>
	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
</head>
<body class="container" style="margin-top: 50px;">

	<% if (messages.error) { %>
	<div class="alert alert-danger" role="alert"><%- messages.error %></div>
	<% } %>

    <div class="card"> 
    	<div class="card-header">
			Add Opintosuoritus
		</div>
        <div class="card-body">
            <form action="/opintosuoritus/add" method="post">
                <div class="form-group">
                    <label>Päivämäärä:</label>
                    <input type="date" class="form-control" name="Paivamaara" value="<%= Paivamaara %>" autocomplete="off">
                </div>
                <div class="form-group">
                    <label>Arviointi:</label>
                    <input type="number" class="form-control" name="Arvosana" value="<%= Arvosana %>" min="0" max="5" autocomplete="off">
                </div>
                <div class="form-group">
                    <label>Opiskelijan id:</label>
                    <input type="number" class="form-control" name="idOpiskelija" value="<%= idOpiskelija %>" min="1" max="5" autocomplete="off">
                </div>
                <div class="form-group">
                    <label>Opintojakson id:</label>
                    <input type="number" class="form-control" name="idOpintojakso" value="<%= idOpintojakso %>" min="1" max="9" autocomplete="off">
                </div>
                <div class="form-group">
                    <input type="submit" class="btn btn-info" value="Add"/>
                </div>
            </form>
        </div>
    </div>

</body>
</html>